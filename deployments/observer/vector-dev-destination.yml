# Dev destination for Vector, sends metrics to Prometheus
sinks:
  prometheus:
    type: prometheus_exporter
    inputs:
      - out-metrics-*
    address: 0.0.0.0:8686
    default_namespace: dev-observer
    flush_period_secs: 60

  # Raw log output to console
  console:
    type: console
    encoding:
      codec: logfmt
    inputs:
      - out-logs-*
